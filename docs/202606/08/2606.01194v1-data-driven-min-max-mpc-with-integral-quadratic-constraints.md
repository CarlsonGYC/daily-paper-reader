---
title: Data-Driven Min-Max MPC with Integral Quadratic Constraints
title_zh: 基于数据的积分二次约束极小极大模型预测控制
authors: "Yifan Xie, Julian Berberich, Frank Allgöwer"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01194v1"
tags: ["query:mppi"]
score: 9.0
evidence: 提出一种考虑不确定性的数据驱动min-max MPC约束处理方法
tldr: 针对数据驱动控制非线性系统保证稳定与约束的挑战，积分二次约束是建模非线性与不确定性的有效工具。本文提出一种数据驱动min-max MPC方法：利用输入状态数据和已知IQC将系统矩阵表示为集合成员形式，进而推导并迭代求解两个半定规划，最小化最坏情况成本上界，获得状态反馈控制律。理论上证明了闭环系统指数稳定且严格满足输入和状态约束，并通过数值仿真验证了有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01194v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 668, \"height\": 891, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01194v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 918, \"height\": 469, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01194v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 905, \"height\": 184, \"label\": \"Table\"}]"
motivation: 数据驱动控制中，如何为含非线性不确定性的未知系统提供严格稳定性和约束保证的min-max MPC方案，是亟待解决的问题。
method: 基于输入状态数据与IQC构建系统矩阵集合成员表示，推导并迭代求解两个最小化最坏成本上界的半定规划，获得状态反馈律。
result: 理论证明闭环指数稳定且满足约束，数值仿真验证了方法的有效性。
conclusion: 该方法为数据驱动非线性系统预测控制提供了一种兼具严格理论保证和数值可行的min-max方案。
---

## 摘要
具有严格保证的非线性系统数据驱动控制是一个具有挑战性的控制问题。积分二次约束（IQCs）为建模非线性特性提供了一个强大的框架。本文提出了一种基于数据驱动的极小极大模型预测控制（MPC）综合方法，该方法利用IQC框架处理受（非线性）不确定性影响的未知系统。通过使用输入-状态数据和IQC知识，未知系统矩阵由集员表示来刻画。我们推导了两个半定规划（SDPs），它们最小化了在所有可能系统动态和不确定性下的最坏情况代价的上界。通过迭代求解这些SDPs，得到了所提出的状态反馈控制律。我们进一步证明了所得的闭环系统是指数稳定的，并且满足输入和状态约束。数值算例验证了所提方法的有效性。

## Abstract
Data-driven control of nonlinear systems with rigorous guarantees is a challenging control problem. Integral quadratic constraints (IQCs) provide a powerful framework for modeling nonlinearities. This paper presents a data-driven min-max model predictive control (MPC) synthesis method for unknown systems subject to (nonlinear) uncertainties using the IQC framework. The unknown system matrices are characterized by a set-membership representation using the input-state data and the knowledge of the IQCs. We derive two semidefinite programs (SDPs) that minimize an upper bound on the worst-case cost over all possible system dynamics and uncertainties. By iteratively solving these SDPs, the proposed state-feedback control law is obtained. We further prove that the resulting closed-loop system is exponentially stable and satisfies the input and state constraints. A numerical example demonstrates the validity of the proposed method.

---

## 论文详细总结（自动生成）

好的，以下是根据论文内容撰写的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义

*   **研究动机与背景**：为具有非线性不确定性且模型未知的动态系统设计控制器，并提供严格的稳定性和性能保证，是控制领域的核心难题。
*   **核心问题**：传统的基于模型的控制方法依赖于精确的系统模型，而现有的数据驱动方法在处理复杂非线性不确定性（尤其是非乘性、非可测类型）时存在局限。
*   **整体含义**：本文旨在填补“数据驱动控制”与“积分二次约束（IQC）框架”之间的空白，提出一种全新的、仅依赖输入-状态数据的鲁棒预测控制方法。该方法能够处理由IQC描述的广泛非线性不确定性，并为闭环系统提供指数稳定和约束满足的理论保证。

### 2. 论文提出的方法论

*   **核心思想**：将IQC建模的非线性不确定性整合到数据驱动的集员表示框架中，并构建一个极小极大模型预测控制（Min-Max MPC）问题，通过最小化所有可能系统动态和不确定性下的最坏情况无限时域成本上界，来求解一个鲁棒的状态反馈控制律。
*   **关键技术细节与算法流程**：
    *   **数据驱动的系统刻画（Lemma 1）**：利用输入-状态数据和IQC的先验知识，将未知的标称系统矩阵$(A, B)$的所有可能取值刻画为一个由二次矩阵不等式定义的集合$\Sigma$。
    *   **问题重构成半定规划（SDP）**：原始的极小极大MPC问题在数学上难以直接求解。作者通过构造一个李雅普诺夫函数上界，将其转化为两个等价但互补的SDP问题（问题10和问题20）。
        *   **问题10**：目标是最小化最坏情况代价的上界$\gamma$，直接求解$P$（李雅普诺夫矩阵）、$F$（反馈增益）和IQC乘子$\Pi, \tilde{\Pi}$。该问题在某些变量上是非线性的。
        *   **问题20**：目标是最小化$\frac{1}{\gamma}$，通过变量替换（如$H = P^{-1}$, $L=FH$）将问题10转化为另一个互补形式。
    *   **迭代求解算法（Algorithm 1）**：由于SDP问题中包含非线性项，作者提出一种迭代交替求解策略。
        1.  首先固定IQC乘子$\Pi$，求解问题20（此时为线性SDP），得到最优的$H, L, \tilde{\Pi}$并计算反馈增益$F$。
        2.  然后固定$F$和上一步得到的$\tilde{\Pi}$，求解问题10（此时为线性SDP），得到最优的$P, \Pi$。
        3.  重复上述步骤直至收敛，从而获得一个局部最优的状态反馈控制律。
    *   **滚动时域实施（Algorithm 2）**：在线运行时，在每个采样时刻$t$，测量当前状态$x_t$，然后调用迭代算法计算最优反馈增益$F_{x_t}$，并仅执行第一步控制输入$u_t = F_{x_t}x_t$。下一时刻重复此过程，以提升控制性能。
*   **理论保证（Theorem 3）**：
    *   **递归可行性**：若问题在初始时刻可行，则其在所有未来时刻均可解。
    *   **指数稳定性**：闭环系统的原点是指数稳定的。
    *   **约束满足**：闭环轨迹严格满足输入和状态的范数约束。

### 3. 实验设计

*   **场景**：一个二维的非线性离散时间系统，其标称动态矩阵$(A_s, B_s)$未知，系统中存在一个满足扇区约束的非线性不确定性$\Delta(z) = 0.01\sin(z)$。
*   **数据集**：使用一段由随机输入（范围为$[-1,1]$）激励系统生成的、长度为$T_f=20$的输入-状态轨迹。
*   **对比方法**：静态状态反馈控制器。该静态增益是在$t=0$时刻由所提算法首次计算得到，并在整个过程中保持不变。
*   **评估指标**：闭环状态和输入轨迹的收敛速度，以及闭环代价的数值大小。

### 4. 资源与算力

*   论文**未提及**任何关于计算硬件（如CPU/GPU型号、数量）或计算时间（如训练或求解时长）的信息。

### 5. 实验数量与充分性

*   **实验数量**：进行了**一组**核心对比实验，展示了所提出的数据驱动Min-Max MPC与静态反馈控制器在同一条闭环轨迹上的性能。
*   **充分性评估**：
    *   **客观公正性**：对比基准（从自身方案衍生出的静态控制器）是合理的，能清晰展示滚动优化带来的性能提升。
    *   **充分性局限**：实验验证明显不足。仅在**一个**低维（二维状态）仿真算例上进行演示，缺乏以下关键验证：
        *   与其他先进数据驱动控制方法的横向对比。
        *   对噪声数据的鲁棒性测试。
        *   对不同类型IQC（如动态IQC、时滞不确定性）的可扩展性验证。
        *   算法中关键参数（如初始乘子$\hat{\Pi}$、数据长度$T_f$）对性能影响的消融研究。
        *   对更高维度、更复杂系统的测试。

### 6. 论文的主要结论与发现

*   论文成功地将IQC框架与数据驱动Min-Max MPC相结合，为模型未知的非线性不确定系统提供了一种有效的控制综合方法。
*   通过将鲁棒控制问题转化为两个可迭代求解的SDP，该方法在计算上是可处理的。
*   理论上严格证明了所提方案能保证闭环系统的指数稳定性和输入/状态约束的满足。
*   数值算例验证了该方法的有效性，并证明相比一次性计算的静态反馈律，滚动时域在线重计算能显著提升闭环性能（闭环代价降低 10.8%）。

### 7. 优点

*   **方法创新**：首次在数据驱动Min-Max MPC框架内成功整合IQC，极大扩展了可处理的非线性不确定性类型（不再局限于乘性或可测量的非线性）。
*   **理论严谨**：提供了包括递归可行性、指数稳定性和约束满足在内的完整闭环理论保证，这是很多数据驱动方法所欠缺的。
*   **计算可行性**：巧妙地将棘手的非凸极小极大问题转化为一系列可交替求解的SDP，提供了清晰的实现算法。
*   **性能提升机制**：滚动时域在线更新反馈增益的策略被证明能有效改善瞬态性能。

### 8. 不足与局限

*   **实验验证严重不足**：仅依赖一个低维、无噪声的简单数值算例，无法充分证明其在复杂、高维或带噪声的真实场景下的有效性与可扩展性。缺乏与现有数据驱动或鲁棒MPC方法的对比是该工作的主要实证缺口。
*   **先验知识要求**：该方法需要已知部分系统矩阵（$B_\omega, C, D$）和表征不确定性的IQC乘子集合$\Pi$。在一些应用中，获取这些精确的先验知识可能具有挑战性。初始乘子$\hat{\Pi}$的选择对算法可行性至关重要，但文中未给出指导原则。
*   **计算开销与可扩展性**：算法每步需要在线迭代求解多个SDP，其计算复杂度与数据长度$T_f$和系统维度相关，可能限制其在快速采样系统或高维系统上的应用。文中未对算法的实时性进行分析。
*   **解的局部最优性**：迭代交替求解方法通常只能保证收敛到局部最优解，无法保证全局最优性。
*   **标准理论限制**：要求数据必须来源于无噪声的真实系统，噪声问题未被考虑。理论保证依赖于初始解的可行性，但这本身并无先验保障。

（完）
