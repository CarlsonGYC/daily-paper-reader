---
title: Distributed Non-Uniform Scaling Control of Multi-Agent Formation with Dynamic Agent Joining
title_zh: 动态智能体加入的多智能体编队分布式非均匀缩放控制
authors: "Tao He, Gangshan Jing"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29191v1"
tags: ["query:mrt"]
score: 9.0
evidence: 具有动态智能体加入的分布式编队缩放控制
tldr: 多智能体编队的非均匀缩放控制可实现沿不同坐标轴以不同比例调整形状，提升复杂环境适应性，但传统方法默认固定成员数量，无法支持动态加入。本文提出一种分布式控制框架，在非均匀缩放机动中动态纳入新智能体，同时维持图拉普拉斯矩阵的谱特性。仿真结果表明该方法能在任意维度有效扩展编队规模，显著增强实际应用中的灵活性与可扩展性。
source: arxiv
selection_source: fresh_fetch
motivation: 现有编队非均匀缩放方法假设固定智能体数量，无法支持动态成员加入，限制了任务扩展性。
method: 提出分布式控制框架，在非均匀缩放过程中动态纳入新智能体，并保持图拉普拉斯谱特性。
result: 仿真验证表明该方法可在任意维度下以分布式方式高效扩展编队，维持缩放机动与谱性质。
conclusion: 该框架突破了固定成员限制，增强了编队在动态环境下的灵活性，适用于多种实际多智能体任务。
---

## 摘要
编队的非均匀缩放控制使多智能体系统能够沿不同坐标轴以不同比例缩放来调整形状，在复杂环境中提供了更高的灵活性。然而，与大多数现有编队机动策略一样，它通常假设智能体集合是固定的，这限制了其在需要动态团队扩展的场景中的适用性。本文提出了一种分布式控制框架，使编队在任意维度的非均匀缩放机动过程中能够纳入新智能体，同时保持图拉普拉斯算子的谱性质。仿真示例验证了理论结果的有效性。

## Abstract
Non-uniform scaling control of formation enables multi-agent systems to adjust their shape by scaling with different ratios along different coordinate axes, offering enhanced flexibility in complex environments. However, like most existing formation maneuver strategies, it typically assumes a fixed set of agents, limiting its applicability in scenarios requiring dynamic team expansion. This paper introduces a distributed control framework that enables a formation to incorporate new agents during non-uniform scaling maneuvers in arbitrary dimensions while preserving the spectral properties of the graph Laplacian. Simulation examples validate the effectiveness of the theoretical results.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机与背景**：编队的非均匀缩放控制允许沿不同坐标轴以不同比例调整形状（如沿走廊方向压缩而不改变其余方向），显著提升在复杂约束环境中的灵活性。现有基于图拉普拉斯的线性编队控制方法大多假定智能体集合固定不变，这限制了在需要动态扩充团队规模（如新机器人临时加入）的开放式多智能体系统中的应用。
- **整体含义**：本文旨在突破固定成员的假设，设计一种分布式框架，使编队在进行非均匀缩放机动的过程中，能够以局部操作动态纳入新智能体，同时保持编队图拉普拉斯矩阵的谱性质（半正定性、核空间对应期望形状空间、跟随者子块正定），从而保证闭环系统的稳定性与编队收敛性。

### 2. 论文提出的方法论

- **核心思想**：利用矩阵值的几何约束与谱图理论，将新智能体与现有编队中两个精心选择的已有智能体构成三元组，构造一个半正定增量矩阵融入原拉普拉斯，使得扩展后的拉普拉斯仍保持所需的谱性质。
- **关键技术与构造**：
  - **矩阵值约束**：对智能体三元组 `(i, j, v)` 定义约束 `W_jk p_ik + W_ki p_jk = 0`，其中权重矩阵由名义构型和旋转矩阵 `R` 生成。该约束在平移与非均匀缩放变换下均保持不变（引理2）。
  - **假设**：要求三元组满足 `∏_{l=1}^d p̃^l_{ji, R} ≠ 0`，以保证新智能体位置可被唯一确定。
  - **正半定拉普拉斯构造**：将新约束 `M_{ijv} p_{ijv} = 0` 嵌入为 `L_{ijv} = M_{ijv}^⊤ D M_{ijv}`，其中 `D ≻ 0` 为设计矩阵。扩展后的拉普拉斯 `L^+ = L_{pad} + L_{ijv}^{pad}`，由原拉普拉斯补零 `L_{pad}` 加上增量组成。
  - **定理6**：严格证明上述构造使 `L^+` 满足 `L^+ ⪰ 0`，核空间为期望形状空间 `Π(p̃^+)`，且跟随者块 `L^+_{ff} ≻ 0`，且新智能体仅需连接两个已有智能体。
- **分布式协议（算法1）**：
  - 新智能体 `v` 感知周围智能体，广播加入请求。
  - 响应者回复名义构型，`v` 自行选择一对 `(i, j)` 满足假设4。
  - 建立双向连接，`v` 与 `i`,`j` 局部计算并更新对应拉普拉斯边权重块（无需全局信息、智能体编号或角色）。增量权重由式（11）直接给出。

### 3. 实验设计

- **实验场景与基准**：
  - 仿真一（构造验证）：二维名义构型，初始6个智能体，`R=I_2`，随后加入第7个智能体，验证拉普拉斯谱性质保持。
  - 仿真二（动态机动）：包含狭窄通道的环境，要求编队执行非均匀缩放才能通过。时间线：0–10 s 初始6智能体形成编队；10 s 时通过协议加入智能体7；15–25 s 执行 y 轴压缩50%的非均匀缩放；20 s 时在缩放过程中再加入智能体8。
  - **未使用标准 benchmark 数据集，也未对比其他方法**；实验为自设场景，主要用于验证本文理论框架的有效性。
- **对比方法**：文中未设置对比方法，仅在引言中讨论了与基于非线性约束、仿射编队等方法的区别，并指出相较于要求连接 `d+1` 个现有智能体的方法，本文仅需连接2个智能体。

### 4. 资源与算力

- 论文通篇未提及任何计算资源、GPU型号、训练时长或具体算力需求。仿真属于小规模数值验证，对算力要求极低。

### 5. 实验数量与充分性

- **实验数量**：共进行两组数值仿真。
- **充分性与客观性**：
  - 实验仅覆盖二维情形，虽理论支持任意维度，但未展示高维结果。
  - 未进行消融实验（如不同三元组选择、设计矩阵 `D` 的影响等）。
  - 未与任何现有动态加入或编队缩放方法进行定量对比，难以评估相对性能。
  - 场景单一（二维、固定障碍物），且加入时刻、位置固定，对更复杂动态加入条件（如同时多个加入、加入失败）未做探讨。
  - 总体而言，实验仅达到概念验证级别，充分性有限，但能够支撑其主要理论主张。

### 6. 论文的主要结论与发现

- 提出了一种基于矩阵值约束的分布式框架，能够在非均匀缩放机动中动态添加新智能体。
- 所构造的拉普拉斯矩阵扩展方法可严格保持半正定性、期望核空间以及跟随者块正定性，为线性控制律的收敛提供保障。
- 分布式加入协议仅需局部交互，新智能体仅连接两个已有智能体，无需全局标识或拓扑角色信息。
- 仿真验证了在狭窄通道等场景下动态扩展编队并完成非均匀缩放的有效性。

### 7. 优点

- **理论严谨**：通过矩阵理论严格证明了扩展拉普拉斯的谱性质保持，适用任意维度。
- **极简实现**：新智能体仅需连接两个现有智能体（与需要 `d+1` 个连接的方法相比），且边权重更新完全分布式。
- **原生支持非均匀缩放**：框架内嵌矩阵值约束，自然兼容沿任意正交轴的非均匀形变。
- **开放多智能体系统适用性**：为需要动态增减成员的任务环境提供了可严格分析的解决方案。

### 8. 不足与局限

- **实验覆盖不足**：仅二维仿真，未验证三维或更高维情形；无消融、无对比实验，难以展现相对于其他方法的优势。
- **假设限制**：要求三元组满足 `∏ p̃^l_{ji,R} ≠ 0`，即连接的两个现有智能体在每个坐标轴上的相对分量均非零，可能限制可选连接对。
- **仅处理加入行为**：未涉及智能体离开、边增删等更一般的拓扑变化；作者在结论中也指出此为未来工作。
- **未考虑实际非理想因素**：如通信延迟、量测噪声、碰撞避免、连接失败重试等，部署到实际系统的鲁棒性待验证。
- **设计矩阵 `D` 的作用未深入挖掘**：文中仅指出 `D≻0` 用作权重自由度，但未分析其对动态响应、鲁棒性的影响。

（完）
