---
title: Sampling-based Model Predictive Control Using Trust Regions
title_zh: 基于信任区域的采样型模型预测控制
authors: "Markus Walker, Marcel Reith-Braun, Daniel Frisch, Uwe D. Hanebeck"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07801v1"
tags: ["query:mppi"]
score: 9.0
evidence: 信赖域方法通过KL散度约束提案分布更新
tldr: 基于采样的模型预测控制（MPC）通过从提议分布采样评估轨迹成本来近似最优控制，但通常依赖启发式调整温度、动量等超参数，导致调参困难且性能不稳定。本文提出信任域约束方法，利用KL散度上限和可选的熵下限限制更新分布变化，并基于拉格朗日函数解析获得最优超参数，取代启发式设计。进一步结合确定性局部累积分布（LCD）采样提高样本利用效率。在基准环境上，该方法在低样本和低迭代次数下收敛更快、样本效率更优，为采样MPC提供了一种无需手动调参、理论有保障的自适应更新范式。
source: arxiv
selection_source: fresh_fetch
motivation: 采样型MPC算法依赖启发式调整超参数，缺乏理论指导，导致调参繁琐且性能难以保证。
method: 提出基于KL散度约束和拉格朗日优化的信任域更新，自动推导最优超参数，并融合确定性LCD采样以提升样本效率。
result: 在低样本和低迭代次数下，方法收敛速度更快，样本效率显著优于传统MPPI。
conclusion: 信任域公式取代启发式调参，为采样MPC提供理论有保障的自适应更新机制，提升可靠性和实用性。
---

## 摘要
基于采样的模型预测控制（MPC）算法，例如模型预测路径积分（MPPI），通过从提议分布中抽取样本、评估轨迹代价并据此更新提议参数，实现了对最优控制问题的近似、无梯度求解。然而，这些方法通常依赖启发式调整超参数，如温度或动量，或进行手动调参。我们提出了一种基于信任区域的采样型MPC公式，通过有原则的Kullback-Leibler（KL）散度上界以及可选的熵下界来约束提议分布的更新。这以相对于底层拉格朗日函数的最优值取代了启发式超参数调整。我们进一步通过将信任区域更新与基于确定性局部累积分布（LCD）的采样相结合，提高了样本效率和收敛性。在两个基准环境上的实验表明，所提出的信任区域更新在低样本和低迭代情况下实现了更快的收敛和更好的样本效率，尤其与基于确定性LCD的采样结合时效果更佳。

## Abstract
Sampling-based model predictive control (MPC) algorithms, such as model predictive path integral (MPPI), enable approximate, gradient-free solutions to optimal control problems by drawing samples from a proposal distribution, evaluating their trajectory costs, and updating the proposal parameters accordingly. However, these approaches typically rely on heuristics for adjusting hyperparameters, such as temperature or momentum, or manual tuning. We propose a trust region formulation for sampling-based MPC that constrains updates of the proposal distribution via a principled Kullback--Leibler (KL) divergence bound and, optionally, an entropy lower bound. This replaces heuristic hyperparameter adaptation with values that are optimal w.r.t. the underlying Lagrangian. We further improve sample efficiency and convergence by combining the trust region update with deterministic localized cumulative distribution (LCD)-based sampling. Experiments on two benchmark environments demonstrate that the proposed trust region update achieves faster convergence and better sample efficiency in low-sample and low-iteration regimes, especially when paired with deterministic LCD-based sampling.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：采样型模型预测控制（MPC）方法（如MPPI、CEM‑MPC）虽然可以并行处理任意代价与动力学，但其性能严重依赖温度、动量这类超参数的启发式调整或手动试凑，缺乏理论指导，导致调参困难且在不同任务上表现不一致。
- **整体含义**：本文提出一种基于信任区域（trust region）的采样MPC框架，用有严格理论基础（拉格朗日对偶）的KL散度约束取代启发式超参数自适应，自动推导每一步更新的最优超参数，从而在低样本、低迭代次数下实现更稳定、更高效的优化。此外，引入确定性LCD采样进一步提升样本利用率。

### 2. 论文提出的方法论
- **核心思想**：将信任区域概念从强化学习迁移到采样MPC：在每次迭代更新提议分布时，限制新旧分布间的KL散度不超过阈值\(\epsilon\)，并可附加熵下界\(H_{\min}\)防止分布过早塌缩。由此构造带约束的优化问题，通过拉格朗日对偶解析求解出最优提议分布的形式。
- **关键技术细节**：
  - 优化问题形式：
    \[
    \min_{f_{j+1}} \mathbb{E}_{f_{j+1}}[J(\bar{\xi})] \quad \text{s.t.} \quad D_{\text{KL}}(f_{j+1}\|f_j)\le \epsilon,\; H(f_{j+1})\ge H_{\min},\; \int f_{j+1}=1.
    \]
  - 拉格朗日对偶求解：得到最优提议为
    \[
    f^*_{j+1}(\bar{\xi}) \propto f_j(\bar{\xi})^{\frac{\eta}{\eta+\alpha}} \exp\!\left(-\frac{J(\bar{\xi})}{\eta+\alpha}\right),
    \]
    其中\(\eta\)、\(\alpha\)分别为KL和熵约束的对偶变量，通过最大化二维对偶函数\(g(\eta,\alpha)\)数值求得。梯度用当前分布\(f_j\)的样本估计。
  - 权重计算：由样本计算权重\(w^{(i)}\propto f_j(\bar{\xi}^{(i)})^{\frac{\eta^*}{\eta^*+\alpha^*}-1}\exp\!\left(-\frac{J(\bar{\xi}^{(i)})}{\eta^*+\alpha^*}\right)\)，再通过加权矩匹配投影回参数族（如高斯），得到更新的均值和协方差。
  - 熵约束自动关闭：若任务不需要，熵约束在优化中自然变为非活跃（\(\alpha^*=0\)），此时权重退化为类似于MPPI的温度加权形式，但温度由信任区域对偶自动确定。
- **采样提升**：将LCD确定性采样与信任区域结合。LCD样本离线优化以最优表示标准正态分布，在线映射到当前高斯提议，并施加随机旋转以保持各向同性下的最优性。这比随机采样和低差异序列（Sobol、Halton）具有更好的覆盖与积分精度。
- **算法整体流程**：包括暖启动、样本裁剪、缓冲重用最佳轨迹、时间相关协方差等常用技巧，与信任区域更新集成。

### 3. 实验设计
- **基准环境**：两个经典控制任务
  - 倒立摆摆起（cart‑pole swing‑up）
  - 卡车倒车入库（truck backer‑upper）
  设置均沿用前作[9]的系统与控制器参数。
- **对比方法**：
  - **启发式基线**：MPPI‑like方法[9]，使用温度启发式[4]和动量平滑(3)更新提议，不含信任区域。
  - **信任区域变体**（末尾加“TR”）：本文方法。
  - 还对比了基于LCD的CEM方法（dsCEM）[8]。
  - 每种框架下测试四种采样策略：随机、Sobol序列、Halton序列、确定性LCD（ds）。
- **评估维度**：
  - 累积代价（代价值求和，越小越好）
  - 控制输入平滑度（相邻控制差分范数之和）
  - 样本效率（固定迭代次数，变化样本数）
  - 收敛性（固定样本数，变化最大迭代次数）
  - 信任区域参数\(\epsilon\)的敏感度（在倒立摆上扫描\(\epsilon\)）

### 4. 资源与算力
- 所有运行时间在**单个 Intel Xeon Platinum 8368 核心**上测量，论文给出了各方法在3次迭代下的单步耗时（毫秒级均值±标准差），如dsTR在倒立摆约202 ms，Random MPPI约166 ms。
- **未提及使用GPU**，也未给出完整训练或评估的总时长术语。整体计算需求不高，适合CPU实时控制场景。

### 5. 实验数量与充分性
- **实验覆盖**：在两个环境上进行了多维度比较：
  - \(\epsilon\)扫描（仅倒立摆，6个\(\epsilon\)值×3种采样，每种100次运行）
  - 样本效率实验（样本数20~300，固定迭代3次，两个环境，5种主要方法组合，各100次运行）
  - 平滑度实验（与样本效率同设定）
  - 收敛性实验（迭代次数1,3,5,10，固定样本40，各100次运行）
  - 计算时间表（一次3次迭代，各100次运行）
  - 采样方法对比涵盖随机、Sobol、Halton、ds四种，以及有无信任区域的交叉。
- **充分性与公平性**：比较基线直接使用同一套算法增强（暖启动、缓冲、时间相关协方差等），仅更新规则不同，对比公平。超参数（\(\epsilon=2\)，\(H_{\min}=-50\)）通过扫描选定，在同一环境下固定后用于所有对比。使用了中位数和四分位距统计，运行100次以减少随机性影响。不过缺乏对更复杂系统、更高维动作空间或非高斯提议的验证。

### 6. 论文的主要结论与发现
- **信任区域更新**在低样本（<100）和低迭代次数（3~10）下，相比启发式MPPI，获得更低的累积代价和更快的收敛，尤其在卡车倒车任务中优势明显。
- **确定性LCD采样**配合信任区域（dsTR）在各项指标上几乎一致最优，兼顾代价降低和动作平滑性。
- \(\epsilon\)参数扫描表明\(\epsilon=2\)是较好的平衡点；熵下界在短迭代中效果不显著，但在更长优化过程中可防止分布塌缩。
- 计算开销略高于启发式方法，但可通过并行化缓解，整体在实时范围内。

### 7. 优点
- **理论严谨**：用带约束的对偶优化完全替代启发式超参数，提供可解释、自适应的更新步长和权重温度。
- **即插即用**：信任区域更新直接嵌入现有采样MPC流程，仅需解决一个二维对偶问题，不会引入高维负担。
- **采样增强**：将LCD确定性采样与信任区域结合，进一步改善了低样本下的积分精度和收敛稳定性。
- **实验详实**：在两种环境、多种采样策略、大量运行次数下，系统对比了代价、平滑度、收敛性和实时性，说服力较强。

### 8. 不足与局限
- **任务规模有限**：仅在两个经典低维控制任务上验证，未在更高维、更复杂动力学的环境中测试（如灵巧手、多足机器人）。
- **\(\epsilon\)仍需设定**：虽然对偶优化了\(\eta,\alpha\)，信任域上界\(\epsilon\)仍需用户指定，论文通过扫描选定，但未给出自动选取策略。
- **熵约束表现不显著**：在短迭代下几乎不发挥作用，其在长时优化中的价值尚未充分展示。
- **分布假设单一**：目前只考虑高斯提议，对多模态或更复杂分布的支持尚未探讨，而投影回高斯会引入误差。
- **计算开销略高**：相比启发式，每步增加了对偶优化，可能影响高频率控制场景（但仍在毫秒级）。

（完）
