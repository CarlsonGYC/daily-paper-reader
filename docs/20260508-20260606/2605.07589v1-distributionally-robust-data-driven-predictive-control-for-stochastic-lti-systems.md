---
title: Distributionally Robust Data-Driven Predictive Control for Stochastic LTI Systems
title_zh: 随机线性时不变系统的分布鲁棒数据驱动预测控制
authors: "Mirhan Urkmez, Shahab Heshmati-Alamdari"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07589v1"
tags: ["query:mppi"]
score: 9.0
evidence: 基于数据的分布鲁棒预测控制与概率约束满足
tldr: 针对动态及扰动分布均未知的随机线性时不变系统，现有的数据驱动预测控制大多缺乏对扰动分布不确定性的鲁棒性保证。本研究提出一种分布鲁棒数据驱动预测控制框架，利用离线轨迹拟合子空间预测器并构建残差经验分布，再围绕其建立Wasserstein模糊集来刻画扰动不确定性。通过最小化模糊集内最差期望成本并施加输出概率约束，问题被转化为一个易处理的直接数据驱动二次规划，无需显式系统辨识。进一步给出数据驱动的Wasserstein半径选择，使得真实成本与约束满足获得高概率保证。数值仿真验证了框架在各种扰动下的鲁棒性和优越性。
source: arxiv
selection_source: fresh_fetch
motivation: 解决未知扰动分布下数据驱动控制的鲁棒性缺失，提供具有有限样本概率保证的控制方案。
method: 采用Wasserstein模糊集对残差经验分布进行鲁棒建模，构建分布鲁棒优化并转化为直接数据驱动形式。
result: 得到可处理的二次规划问题，并提供数据驱动半径的有限样本概率上界与约束满足保证。
conclusion: 所提框架兼具计算易处理性与严格概率保证，在鲁棒性和性能上优于现有数据驱动预测控制方法。
---

## 摘要
我们针对动力学特性和干扰分布未知的随机线性时不变系统，提出了一种分布鲁棒数据驱动预测控制框架。我们利用离线轨迹通过最小二乘拟合子空间预测控制（SPC）预测器，并构建预测残差的经验分布作为未知干扰分布的代理。接着，围绕该估计构造一个Wasserstein模糊集，并最小化最坏情况下的期望代价，同时强制满足该模糊集中所有分布下的概率输出约束。由此产生的问题可转化为一个等价的直接数据驱动形式，从而无需显式的预测器辨识。借助有限样本集中结论，我们提供了一个数据驱动的Wasserstein半径，使得在真实干扰分布下，真实期望代价以高概率被该可处理目标上界所约束，并且输出约束得以满足。数值仿真在不同干扰条件和代价函数下，验证了该框架相对于现有方法的有效性。

## Abstract
We propose a distributionally robust data-driven predictive control framework for stochastic linear time-invariant systems with unknown dynamics and disturbance distributions. We use an offline trajectory to fit the subspace predictive control (SPC) predictor via least squares and construct an empirical distribution of the prediction residuals as a proxy for the unknown disturbance distribution. We then center a Wasserstein ambiguity set around this estimate and minimize the worst-case expected cost while enforcing probabilistic output constraint satisfaction over all distributions in the set. The resulting problem admits a tractable reformulation with an equivalent direct data-driven form, eliminating the need for explicit predictor identification. Using finite-sample concentration results, we provide a data-driven Wasserstein radius such that, with high probability, the true expected cost is bounded above by the tractable objective and output constraints are satisfied with respect to the true disturbance distribution. Numerical simulations validate the framework against existing methods under various disturbance conditions and cost functions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：针对动力学模型与扰动分布均未知的随机线性时不变（LTI）系统，现有的数据驱动预测控制（DDPC）方法大多缺乏对扰动分布不确定性的鲁棒性机制。当系统受到随机干扰时，直接使用汉克尔矩阵的参数化不再精确，导致预测偏差和约束违反。
- **核心问题**：如何在不假定扰动分布已知、且仅依赖单条离线轨迹的条件下，设计一种既能最小化期望代价，又能以高概率满足输出约束的数据驱动预测控制器。
- **整体含义**：本文提出了一种分布鲁棒数据驱动预测控制（DR-DDPC）框架，通过构建基于Wasserstein距离的模糊集来刻画预测残差分布的不确定性，将问题转化为一个可处理的直接数据驱动优化问题，并给出有限样本概率保证。

### 2. 论文提出的方法论
- **核心思想**：
  - **两步构建模糊集**：首先用离线数据通过最小二乘估计一个子空间预测控制（SPC）多步预测器\(\hat{K}\)，并利用预测残差构建经验分布\(\hat{P}_{y_f|m_f}\)；然后以该经验分布为中心、以Wasserstein球作为模糊集，覆盖未知的真实输出分布。
  - **分布鲁棒优化**：在模糊集上最小化最坏情况下的期望代价，并强制满足条件风险价值（CVaR）约束（等价于概率输出约束）。
  - **直接数据驱动等价**：通过等价变换，将需要显式\(\hat{K}\)的优化问题转化为仅依赖离线汉克尔矩阵和当前测量数据的二次规划，省去了系统辨识步骤。
- **关键技术细节**：
  - **SPC残差生成**：\(\hat{\xi}_f^{(i)} = (Y_f - \hat{K}M)e_i\)，其中\(M\)由离线输入/输出数据组成，\(e_i\)为标准基向量。
  - **Wasserstein模糊集**：\(P = \{Q : d_{W_r}(Q, \hat{P}_{y_f|m_f}) \le \varepsilon\}\)，利用Wasserstein度量的对偶表示进行可处理重构。
  - **可处理重构（定理1）**：对于Lipschitz连续的凸代价函数\(f_2\)和约束函数\(h\)，最坏情况期望转化为经验平均加上一个与Lipschitz常数和半径\(\varepsilon\)成正比的项；CVaR约束则通过引入松弛变量转化为线性不等式组。
  - **直接数据驱动形式（定理2）**：令决策变量\(g\)满足\(U_p g = u_p\), \(Y_p g = y_p\), 且\((I-P)g=0\)（\(P=M^\dagger M\)），则预测输出等价于\(Y_f g + Y_f (I-P)e_i\)，从而得到完全基于数据的优化问题（18）。
  - **有限样本保证（定理3）**：利用集中不等式和\(m\)-依赖性质，推导出一个数据驱动的Wasserstein半径\(\varepsilon(\alpha, m_f) = \varepsilon_1(\alpha) (\frac{1}{N}\sum_i \|M e_i - m_f\|_r^r)^{1/r} + \varepsilon_2(\alpha)\)，使得真实分布以概率\(1-\alpha\)落入模糊球内，从而保证真实期望代价被可处理目标上界所约束，且CVaR约束得以满足。
- **算法流程**：滚动时域控制中，在每一步求解直接数据驱动优化问题（18），应用所得控制序列的首个输入，测量新输出，更新过去窗口后重复。

### 3. 实验设计
- **系统与场景**：采用文献[2]中的二阶LTI系统，含过程噪声和测量噪声。系统受限于输入和输出盒约束（±2）。控制目标为跟踪正弦参考信号或满足输出约束[0,2]。
- **Benchmark方法**：
  - **SPC**：使用最小二乘估计的\(\hat{K}\)进行预测的间接数据驱动预测控制。
  - **Reg-DeePC**：带有\(\ell_1\)正则化的直接数据驱动预测控制（源自[5]），正则化权重通过网格搜索确定。
  - **DR-DDPC**：本文提出的分布鲁棒直接数据驱动预测控制（问题18）。
- **数据生成与设置**：离线轨迹长度\(T=200\)，输入为随机高斯信号；过去时域\(T_p=5\)，预测时域\(T_f=10\)，残差样本数\(N=186\)。所有实验均为50次蒙特卡洛仿真，同一组随机种子共享给所有方法。
- **参数扫描实验**：考察约束半径\(\varepsilon_{con}\)（从\(10^{-5}\)到\(1\)）和风险水平\(\beta\)（0.1到0.9）对平均约束违反率和性能代价的影响，同时与SPC、Reg-DeePC的违反率对照。
- **噪声与代价鲁棒性实验**：在不同高斯创新项协方差（SNR 10dB, 20dB, 40dB）和两种均值（零均值与偏置0.05）下，比较二次代价的跟踪性能。随后，更换输出代价函数为\(\ell_1\)范数和不对称线性代价（超调惩罚加倍），检验对分布形状的敏感性。

### 4. 资源与算力
- 文中未明确提及使用的GPU类型、数量、单次训练或推理的时长。仅给出一个代码仓库链接（https://github.com/mirhanu/DR-DDPC），并说明所有方法在相同蒙特卡洛迭代中运行。因此，**关于算力的具体信息并未提供**，但从问题规模（\(T_f=10, N=186\)）推断，该二次规划在标准桌面CPU上即可完成求解，无需大规模并行计算。

### 5. 实验数量与充分性
- **实验组数**：
  - 参数扫描：\(6\times5\)（6个\(\varepsilon_{con}\)值 × 5个\(\beta\)值） = 30种配置，每种50次蒙特卡洛，共计1500次运行。
  - 噪声鲁棒性：3种协方差水平 × 2种均值情况（零均值与偏置0.05）= 6种场景，每种场景对比3种方法（SPC, DR-DDPC, Reg-DeePC），50次蒙特卡洛。
  - 代价函数鲁棒性：3种协方差水平 × 2种非二次代价（\(\ell_1\)和不对称）= 6种场景，同样3方法对比，50次蒙特卡洛。
- **充分性与公正性**：
  - 实验覆盖了参数敏感性、不同噪声规模与偏置、不同代价函数类型，设计较为全面。
  - 对比方法选取得当（标准SPC和正则化DeePC），且共享同一组随机种子，保证了公平比较。
  - 所有结果均报告均值和标准差，增强了统计可靠性。
  - 实验仅在一个特定数值系统上进行，缺乏跨系统或针对更复杂约束的验证，但在论文范围内可以接受。

### 6. 论文的主要结论与发现
- **零均值高斯噪声 + 二次代价**：DR-DDPC与SPC的性能几乎一致，因为此时分布鲁棒目标退化为期望场景，而残差均值为零使得经验平均与SPC预测重合。Reg-DeePC性能显著差于前两者。
- **非零均值噪声**：当创新项存在正偏置时，DR-DDPC明显优于SPC，因为它能通过模糊集补偿预测器的偏差，而SPC无法修正残差均值不为零带来的系统误差。
- **非二次代价函数**：在\(\ell_1\)成本或不对称成本下，DR-DDPC相比SPC的优势被放大，因为这类代价对输出的整个分布形状敏感，而不仅仅依赖于均值预测。
- **约束处理**：通过调节模糊集半径\(\varepsilon_{con}\)和风险水平\(\beta\)，可以有效控制输出约束的违反率。较大的\(\varepsilon_{con}\)使约束更保守，降低违反率但增大性能代价；增大\(\beta\)则允许更多违反，代价降低。在所有测试中，经验违反率均控制在预设的\(\beta\)水平以下。
- **理论保证得到验证**：有限样本半径公式虽然保守，但指明了方向，且通过手动调节半径仍能获得可靠的闭环性能。

### 7. 优点
- **理论严密**：首次为数据驱动预测控制提供了基于Wasserstein模糊集的分布鲁棒性框架，并给出完整的有穷样本概率保证（代价上界和约束满足）。
- **可处理性与等价性**：证明了分布鲁棒问题可转化为简洁的二次锥或二次规划，且存在等价的直接数据驱动形式，无需显式辨识系统矩阵，实现真正端到端的数据驱动控制。
- **计算高效**：利用残差样本的平均和线性约束，避免了随机规划中常见的复杂采样或场景切割。
- **适应性强**：对代价函数仅要求凸性和Lipschitz连续性，可覆盖二次、线性、不对称惩罚等实际需求；对噪声分布无参数假设。
- **实验对比充分**：系统地展示了方法在不同噪声特性、不同代价函数下的表现，并清晰揭示了相较于SPC和正则化DeePC的优势场景。

### 8. 不足与局限
- **半径过于保守**：理论推导出的\(\varepsilon(\alpha, m_f)\)在算例中被认为过于保守，实际应用时需将其拆分为独立的调节参数，这降低了理论半径的直接实用性。
- **系统设定单一**：所有实验仅基于一个二阶SISO系统，且创新项为高斯分布或有偏高斯，缺乏对更复杂分布（如重尾噪声、非加性噪声）或更高维多变量系统的验证。
- **未考虑递归可行性与稳定性**：文章明确指出，闭环稳定性与递归可行性保证是未来工作，目前仅是开环优化视角。
- **有限样本分析的局限性**：概率保证依赖于若干假设，如\(\|K-\hat{K}\|_r\)的高概率界\(\gamma(\alpha)\)已知、残差的\(m\)-依赖/\(\rho\)-混合条件成立，这些在实践中可能难以精确验证。
- **计算复杂度与残差数量**：问题规模依赖于离线残差样本数\(N\)（成本项使用全部186个，约束项使用了20个），当离线数据很长时，优化变量数量会线性增长。
- **仅考虑CVaR约束**：概率约束仅通过CVaR这一保守近似实现，未与其他分布鲁棒机会约束形式进行对比。

（完）
