---
title: Coordinated Motion Planning is FPT on Discretized Simple Polygons
title_zh: 离散化简单多边形上的协同运动规划是固定参数可解的
authors: "Argyrios Deligkas, Eduard Eiben, Robert Ganian, Iyad Kanj"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07570v1"
tags: ["query:mrt"]
score: 9.0
evidence: 多机器人协调运动规划以最小化总行程长度的固定参数可解算法
tldr: 在协调运动规划中，k个机器人需在图中无碰撞地从起点移动到终点，目标是最小化总旅行长度。该问题一般NP-hard，仅在特殊图类如全网格和树宽有界图上被证明为固定参数可解（FPT），参数为k。本文研究简单多边形离散化所得图类，提出固定参数算法，证明在此类图上问题亦属于FPT。此类图不仅广泛应用于机器人运动规划，且概括矩形网格，是向解决子网格和平面图等开放问题迈出的关键一步。
source: arxiv
selection_source: fresh_fetch
motivation: 实际机器人运动常需在离散化多边形环境中规划，而该图类的参数化复杂性尚未解决。
method: 基于参数k，在由简单多边形离散化生成的图上，通过分析图结构性质，构造固定参数算法。
result: 证明协调运动规划在该图类上关于k是固定参数可解的（FPT）。
conclusion: 该结果将FPT结果推广至更泛用的多边形离散图，为子网格和平面图情形提供了理论基础。
---

## 摘要
在协同运动规划问题中，给定一个图以及k个机器人的起始顶点和目标顶点。在每个时间步，任意子集的机器人可以移动，每个机器人穿越图的一条边，前提是没有两个机器人发生碰撞。目标是在最小化某个目标函数的同时，计算出将所有机器人送达目的地的调度方案。本文聚焦于最小化所有机器人总行程这一被广泛研究的目标。该问题已知是NP难的，并且在满网格(SoCG 2023)和有界树宽图(ICALP 2024)上，当以机器人数量k为参数时，已被证明是固定参数可解(FPT)的。我们提出了一种针对由简单多边形离散化所得图上的协同运动规划的固定参数算法，参数为机器人数量k。这类图在实际应用中特别受关注，因为平面运动通常受限于多边形环境的离散化表示。此外，这些图是矩形网格的推广；因此，我们的结果是朝着解决子网格乃至最终平面图上协同运动规划的参数复杂性这一领域的两个突出开放问题迈出的重要一步。

## Abstract
In the coordinated motion planning problem, we are given a graph together with the starting and destination vertices of $k$ robots. At each time step, any subset of robots may move, each traversing an edge of the graph, provided that no two robots collide. The goal is to compute a schedule that routes all robots to their destinations while minimizing some objective function. In this paper, we focus on the well-studied objective of minimizing the total travel length of all robots. This problem is known to be NP-hard, and it has been shown to be fixed-parameter tractable (FPT), when parameterized by the number $k$ of robots, on full grids (SoCG 2023) and on bounded-treewidth graphs (ICALP 2024).   We present a fixed-parameter algorithm for coordinated motion planning, parameterized by the number $k$ of robots, on graphs arising from discretizations of simple polygons. Such graphs are of particular interest in real-world applications, where planar motion is often constrained to discretized representations of polygonal environments. Moreover, these graphs generalize rectangular grids; consequently, our result constitutes a significant step toward resolving the parameterized complexity of coordinated motion planning on subgrids and, ultimately, planar graphs -- two prominent open problems in the field.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题定义**：研究**协同运动规划（Coordinated Motion Planning, CMP）** 及其推广 **GCMP**，目标是在图中调度k个机器人从起点到终点，要求无碰撞（不能同时占同一顶点或对穿同一条边），且**最小化所有机器人的总旅行长度（总能量）**。
- **已知复杂性**：该问题一般NP-hard；在**全矩形网格**（SoCG 2023）和**有界树宽图**（ICALP 2024）上以机器人数量k为参数是固定参数可解（FPT）的。
- **动机与核心问题**：实际多机器人运动（如仓库、交通）常需将连续多边形环境离散化为网格图，这类图是**简单多边形的离散化图（discretized simple polygons）**，严格推广了矩形网格。此前，CMP在子网格和平面图上的参数化复杂度仍是公开难题。本文旨在**证明CMP在离散化简单多边形上关于k是FPT**，从而填补这一空白并推动对更广泛平面图类的研究。

### 2. 论文提出的方法论：核心思想与关键技术细节
论文提出一个全新的FPT算法，核心策略是**将输入图约简为树宽受k界定的图，再调用已有的参数化算法（k+树宽）求解**。算法包含四大步骤：

- **步骤0：预处理**
  - 利用已有结果（Theorem 3），将GCMP实例多项式时间归约为CMP实例，并且可以假设每个机器人的**旅行冗余（travel slack）** 不超过λ = O(k^5)，且在全矩形子网格内的**转弯次数和等待点数**受μ = k^{O(k^2)}约束（Assumption 5）。

- **步骤1：扇区（Sectors）与扇区图**
  - 针对每个端口（起点/终点正交方向对）定义**弯曲距离（bend distance）**，据此构造**弯曲向量**，将图划分为同弯曲向量的最大连通子图——**扇区**。
  - 扇区图Γ以扇区为顶点，邻接关系为边。证明了关键结构性质：
    - Γ的树宽 ≤ 7^{4k-1}（Theorem 16）。
    - 每条直线路径至多穿过12k个扇区（Lemma 11）。
    - 每个扇区至多有8个“非干净”邻居（Lemma 17）。
  - 扇区按基线结构分成**直路径、直方图（histogram）、阶梯（staircase）、矩形（rectangle）** 四类，并给出各自基线分离性质。

- **步骤2-3：约简规则**
  - 针对不同扇区类型设计保等价约简，移除或替换不可能被任何最优解使用的顶点：
    - **直方扇区**：删除距离基线超过λ/2的顶点。
    - **清洁扇区**：类似删除过远顶点。
    - **阶梯扇区**：以基线交点为中心，构建宽度为(k+1)(λ+1)的缓冲区，证明任意解可修改为不离开缓冲区的解（引入广场Ω和重路由），删除缓冲区外的顶点。
    - **矩形扇区**：利用转弯和等待的μ界，构造“第一帧”和“第二帧”，证明存在**规范解**：在内部第二帧区域内机器人不转弯、不等待，且仅沿直线通过。基于此，删除内部网格顶点，用**编织路径（degree-2 weaving paths）** 连接对边，虽破坏平面性但大幅降低树宽。
  - 所有规则不会改变实例的可满足性（yes/no 等价）。

- **步骤4：树宽有界性证明**
  - 约简后，每个扇区的高度或宽度被控制在某函数q(k)内。利用“特殊顶点”分割扇区，保证移除特殊顶点后每个连通分支是直线路径经过的≤12k个扇区的子图，且树宽受O(k·q(k))界。
  - 将Γ的树分解提升为原图G的树分解，最终证明**约简后G的树宽 ≤ 7^{28k+5}·q(k)^3 = k^{O(k^2)}** (Theorem 25)。
  - 再调用已知的FPT算法（参数为k+树宽）解决CMP。

### 3. 实验设计
**本文是纯理论论文，未包含任何实验评估。** 所有贡献均以定理、引理的形式给出严格的数学证明，包括结构性质、约简规则的等价性、树宽上界等。因此，没有数据集、基准、对比方法。

### 4. 资源与算力
**文中未提及任何计算资源或算力信息。** 算法仅描述理论上的运行时间，为FPT时间 f(k)·n^{O(1)}，但未给出具体实现或实验所需的硬件配置。

### 5. 实验数量与充分性
由于没有实验部分，不涉及实验数量、公平性或充分性评估。理论证明通过组合与图论技巧严格保证正确性。

### 6. 论文的主要结论与发现
- **主要定理**：**GCMP（从而CMP）在离散化简单多边形图上是固定参数可解的，参数为机器人数量k**（Theorem 1）。该算法是构造性的，若存在总长度≤ℓ的调度方案，则能输出一个。
- **方法创新**：明确提出并证明了通过“编织”替换顶点（而非仅删除无关顶点）的树宽约简技术，可能为其他几何图问题提供新思路。
- **推广意义**：该结果将FPT性从满矩形网格扩展到更一般的离散化简单多边形（即简单连接多边形），是朝着解决子网格和平面图CMP问题迈出的重要一步。

### 7. 优点
- **突破性**：解决了离散化多边形上的CMP这一此前公开的问题，拓宽了FPT图类。
- **新颖的树宽约简**：不同于传统的“无关顶点删除”或直接利用网格属性，本文通过扇区分析和分类处理，对矩形扇区引入“编织路径”替换内部顶点，虽产生非平面图但成功降低了树宽。这一技术具有启发性。
- **结构洞察深刻**：扇区的弯曲距离定义、基线性质及其与端口分离的关系，得到了非常清晰的结构刻画（如扇区图树宽界、直线路径扇区数界）。
- **完备的理论证明**：所有规则均给出了等价性证明，并对解进行了一系列重路由和规范化，确保了算法正确性。

### 8. 不足与局限
- **纯粹理论性，无实验验证**：未提供任何实现或计算实验，无法评估实际运行效率或常数因子。f(k)可能极大，实用性有限。
- **图类限制**：仅限于**简单多边形**（无孔洞）的离散化图。尽管作者提到可推广到“单个孔洞的满网格”，但对一般多个孔洞或更复杂的平面子图尚未解决。
- **参数限制**：仅以机器人数量k为参数，没有考虑其他结构化参数。虽然结果以k为FPT，但函数f(k)为 k^{k^{O(k^2)}} 级别增长极快，限制了中小规模实例的可用性。
- **方法复杂性**：扇区分区、多种约简规则、重路由论证等组合在一起相当繁重，可能不易于直接复现或教学传播。

（完）
