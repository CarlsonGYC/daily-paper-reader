---
title: Model Predictive Control for Constrained Linear Positive Systems on Graphs
title_zh: 图上约束线性正系统的模型预测控制
authors: "Roland Schurig, David Ohlin, Anders Rantzer, Emma Tegling, Rolf Findeisen"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28550v1"
tags: ["query:mppi"]
score: 9.0
evidence: 约束线性正系统的模型预测控制
tldr: 正系统在路由、物流等网络中建模时，状态与输入均为非负，加入容量约束后，模型预测控制问题复杂度显著增大。本文利用无约束问题解析结构，构造显式次优容许控制器，得到基于图计算的性能界和最小稳定预测时域，无需终端条件；并通过凸规划高效计算最优界与时域。结果揭示了系统结构如何提供传统MPC难以获得的显式保证。
source: arxiv
selection_source: fresh_fetch
motivation: 正系统在容量约束下MPC复杂度剧增，亟需显式保证与可计算性能界。
method: 利用无约束问题解析结构构造显式次优控制器，推导图计算性能界和最小稳定时域，并通过凸规划求解。
result: 得到无终端条件的MPC稳定保证及最优性能界和时域，均可基于图结构高效计算。
conclusion: 系统结构使得传统MPC难以获得的显式保证成为可能，凸显图结构利用价值。
---

## 摘要
描述具有本质非负状态和输入的网络的正系统自然地出现在路由、物流和分区建模中。我们考虑建模为关联形式正线性系统且具有线性成本的问题。对状态（存储）和输入（节点间流量）施加容量约束显著增加了问题复杂性。利用无约束问题的解析结构，构造了一个显式的次优容许控制器。这为无终端条件的模型预测控制器提供了图可计算的性能界限和最小稳定时域长度。一个凸规划使得最优界限和时域的高效计算成为可能。这些结果突显了系统结构如何使得通常无法获得的显式MPC保证成为可能。

## Abstract
Positive systems describing networks with inherently non-negative states and inputs arise naturally in routing, logistics, and compartmental modelling. We consider problems modelled as positive linear systems in incidence form with linear cost. The addition of capacity constraints on states (storage) and inputs (flows between nodes) significantly increases the problem complexity. Leveraging the analytic structure of the unconstrained problem, an explicit suboptimal admissible controller is constructed. This yields graph-computable performance bounds and a minimum stabilising horizon length for a model predictive controller without terminal conditions. A convex program enables efficient computation of the optimal bound and horizon. These results highlight how system structure enables explicit MPC guarantees that are typically not available.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：正系统（状态和输入均为非负）广泛用于描述路由、物流及分区网络中的库存与流量动态。在这类网络上进行最优控制（例如将所有物资导向目标节点）是一类经典问题。
- **核心难点**：当对节点的存储容量（状态约束）和边的传输容量（输入约束）施加盒式上界时，原有的无约束显式最优解不再适用，问题复杂度显著升高。
- **整体目标**：为带容量约束的正系统设计一种无终端条件的模型预测控制（MPC）方案，并利用系统结构给出**显式的、可图计算的稳定性保证和无限时域性能界**，这在一般约束线性系统中通常难以获得。

### 2. 论文提出的方法论
- **核心思想**：
  - 利用无约束条件下最优控制问题的解析解（存在线性最优值函数 \(V_\infty(x)=p^\top x\) 和静态反馈律 \(\mu(x)=Kx\)，其中每个节点将所有物资沿唯一最优后继边发送）。
  - 将该最优反馈律改造为**次优但容许的控制器** \(\hat{\mu}(x)=K\Lambda x\)，通过在各节点引入缩减因子 \(\lambda_i\in(0,1]\) 来避免违反容量约束。
  - 基于该构造，推导出有限时域最优值函数 \(V_N(x)\) 的上界 \(V_N(x)\le \gamma s^\top x\)，进而利用 Grüne 和 Pannek 的定理给出**MPC 无终端条件稳定所需的充分预测时域下界**以及**闭环成本与无限时域最优成本的比值 \((1/\alpha_N)\) 的显式表达**。

- **关键技术细节与公式（用文字概括）**：
  - **系统模型**：线性正系统 \(x(t+1)=x(t)+Bu(t)\)，其中 \(B\) 为图的关联矩阵，状态和输入均非负且受盒式约束 \(x\le\bar{x},\,u_i\le\bar{u}_i\)，瞬时成本 \(\ell(x,u)=s^\top x+r^\top u\)。
  - **无约束最优解回顾**（定理 1）：若存在 \(p>0\) 满足 \(s+\sum_i\min\{r_i+B_i^\top p,0\}e_i=0\)，则 \(V_\infty(x)=p^\top x\)，且最优反馈可写成 \(Kx\)，\(K\) 由每个节点选择成本最小的输出边构成。
  - **可行次优控制器的构造**：定义 \(\hat{\mu}(x)=K\Lambda x\)（\(\Lambda=\operatorname{diag}(\lambda)\)）。通过求解关于 \(\lambda\) 的可行性条件 \(K\Lambda\bar{x}\le\bar{u}\) 且 \(BK\Lambda\bar{x}\le 0\)，保证 \(\hat{\mu}\) 满足所有状态和输入容量约束（命题 1）。该控制在图上仍沿最优路径，但每步只转移一部分物资。
  - **性能界的推导**：计算 \(\hat{\mu}\) 下的闭环无限时域成本为 \(\hat{p}^\top x\)，其中 \(\hat{p}=- (BK)^{-\top}(\Lambda^{-1}s+K^\top r)\)（引理 2）。于是得到 \(V_N(x)\le \hat{p}^\top x\le \gamma s^\top x\)，其中 \(\gamma=\max_i\{\hat{p}_i/s_i\}\)（命题 2），从而满足 MPC 稳定性定理所需的有界条件。
  - **预测时域与性能保证**（定理 2）：若 \(N\) 大于由 \(\gamma\) 决定的某个显式下界，则 MPC 闭环全局渐近稳定，且闭环成本满足 \(J_\infty^{\mathrm{cl}}(x_0,\mu_N)\le V_\infty(x_0)/\alpha_N\)，\(\alpha_N\) 也由 \(\gamma\) 和 \(N\) 显式给出。
  - **最优界与时域的凸计算**：将寻找最小 \(\gamma\) 的问题建模为一个关于变量 \((\gamma,\lambda)\) 的**几何规划**（命题 3），可高效求出最优性能界和最小稳定时域。

### 3. 实验设计
- **实验场景与对象**：仅使用了一个具体的 5 节点（加 1 个目标节点）的有向图示例（Example 1），其中严格指定了状态成本 \(s\)、输入成本 \(r\)、状态上界 \(\bar{x}\) 和输入上界 \(\bar{u}\)。
- **对比方法**：将本文提出的 **MPC 反馈律 \(\mu_N\)**（\(N=16\)）与构造的**次优容许反馈律 \(\hat{\mu}\)** 进行仿真对比，没有采用额外的终端条件或其他 MPC 变体作为基准。
- **数据集/基准**：没有使用标准数据集或与既有文献中的其他 MPC 方法定量比较。

### 4. 资源与算力
- 论文未提及任何计算资源（如 GPU 型号、数量、训练时间等），因为这是一篇理论控制论文，不涉及机器学习模型的训练，计算仅涉及一个数值例子和一次几何规划的求解。

### 5. 实验数量与充分性
- **实验数量**：全篇仅给出 **1 组数值模拟**（约 1 个初始状态 \(\bar{x}\)，展示状态轨迹和输入轨迹）。
- **充分性与公平性**：该实验仅用于**理论观点的演示**，证明 MPC 有限时间驱至原点且成本优于次优控制器 \(\hat{\mu}\)。未进行多初始状态测试、不同约束水平、不同网络规模或与其他 MPC 策略（例如带终端成本/终端集）的对比，因此实验不足以从统计上验证方法的普遍性，也未能展现相对其他方法的优势。但从理论文献角度看，其主要贡献在于分析而非实验。

### 6. 论文的主要结论与发现
- 利用正系统和图结构的解析性质，可以构造出**显式的、满足盒式容量约束的次优线性反馈律**。
- 以该次优控制器为中介，能够导出 **MPC 无终端条件稳定所需的充分预测时域下界** 以及 **闭环性能与最优无限时域性能的严格显式比值**。
- 上述稳定时域和性能界可通过一个**凸几何规划**高效计算，提供了“图可计算”的保证。
- 在示例中，MPC 因能分岔流量而避开瓶颈，性能明显优于结构固定的次优控制器。

### 7. 优点
- **理论贡献明确**：对于一类特殊结构但富有应用背景的系统，提供了通常难以获得的、**可显式计算** 的 MPC 稳定性与性能保证。
- **利用结构精巧**：将无约束最优解的静态路径特性（去冗余边）与缩放因子结合，在保持部分最优结构的同时确保可行性。
- **计算上可处理**：将最优设计问题表述为几何规划，避免了暴力搜索或保守的线性规划松弛。
- **无终端条件方案的优势**：避免了终端约束带来的吸引域缩小和性能估计困难，且证明中直接给出了闭环成本相对于 \(V_\infty\) 的近似比。

### 8. 不足与局限
- **实验极度匮乏**：仅仅一个 5 节点示例，完全未测试网络规模变化、不同拓扑、不同成本或约束参数的敏感度，难以支撑方法的一般实用性宣称。
- **保守性未量化**：由次优控制器得到的性能界 \(\gamma\) 可能相当保守（示例中 \(\gamma=6.4\) 导致最小稳定时域为 12），但文中未讨论这一保守度在实际问题中的严重性。
- **对比基准缺失**：没有与传统的带终端约束/终端成本的 MPC 或其他路由优化算法在性能、计算时间上进行比较，削弱了方法的优越性说服力。
- **应用场景假设强**：模型假设目标节点将物资移出网络（对应 B 的特定结构），且成本均为线性；更一般的需求、多商品或非线性成本情况未讨论。稳定性分析依赖于 nilpotent 矩阵性质，仅当所有物资最终可抵达目标时才成立。
- **可扩展性未知**：虽然几何规划理论可解，但大型网络的约束数快速增长，文中未讨论求解的可扩展性或可能的近似方案。

（完）
