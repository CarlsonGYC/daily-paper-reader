---
title: Generative Multi-Robot Motion Planning via Diffusion Modeling with Multi-Agent Reinforcement Learning Guidance
title_zh: 通过扩散建模与多智能体强化学习引导的生成式多机器人运动规划
authors: "Suk Ki Lee, Venkata Sai Deepak Mutta, Hyunwoong Ko"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00933v1"
tags: ["query:marl-robot"]
score: 9.0
evidence: 结合扩散生成与多智能体强化学习协调的分布式多机器人运动规划
tldr: "多机器人共享环境中，集中式运动规划难以扩展，而分散式方法缺乏交互协调。本文提出结合扩散模型与多智能体强化学习（MARL）的框架：各机器人通过单智能体扩散模型独立生成候选轨迹，再经集中式MARL价值函数梯度引导去噪实现协调。在四机器人迷宫实验中，该方法将干扰率从55.4%降至41.8%，证明无需联合训练即可在分散生成规划中有效引入协调，保持可扩展性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00933v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 2492, \"height\": 2135}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-002.webp\", \"caption\": \"\", \"page\": 8, \"index\": 2, \"width\": 2441, \"height\": 1265}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-003.webp\", \"caption\": \"\", \"page\": 8, \"index\": 3, \"width\": 2542, \"height\": 891}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-004.webp\", \"caption\": \"\", \"page\": 8, \"index\": 4, \"width\": 856, \"height\": 833}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-005.webp\", \"caption\": \"\", \"page\": 8, \"index\": 5, \"width\": 1871, \"height\": 850}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-006.webp\", \"caption\": \"\", \"page\": 10, \"index\": 6, \"width\": 1672, \"height\": 1622}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-007.webp\", \"caption\": \"\", \"page\": 10, \"index\": 7, \"width\": 1683, \"height\": 1614}]"
motivation: 分散式多机器人规划缺乏交互协调，集中式方法难以扩展，急需兼顾协调与可扩展性的新方法。
method: 各机器人利用单智能体扩散模型独立生成轨迹，通过集中式MARL价值函数梯度引导反向去噪过程，实现交互感知协调。
result: "在四机器人迷宫实验中，引导扩散规划将干扰率从55.4%降至41.8%，实现显著协调提升。"
conclusion: 研究表明MARL价值引导可有效将协调引入分散生成规划，无需完全联合模型，保持可扩展性。
---

## 摘要
共享环境中的多机器人协调需要为每个智能体生成可行轨迹，同时考虑智能体间的交互。随着机器人数量增加，集中式规划方法难以扩展，而允许每个智能体独立规划的分散式方法本身并不考虑智能体间的交互。本文提出了一个协调多机器人运动规划的框架，该框架结合了分散式生成轨迹规划与基于多智能体强化学习的协调。每个机器人使用在单智能体运动数据上训练的扩散模型独立生成候选轨迹，利用生成模型产生多样且可行轨迹的能力。为减少智能体间的冲突，通过多智能体强化学习训练的集中式价值函数通过基于梯度的引导来引导逆向扩散过程，从而在不进行集中联合规划或重新训练生成模型的情况下实现交互感知的轨迹生成。该引导遵循指数倾斜公式，其中价值函数使去噪分布偏向于具有更高预期多智能体回报的轨迹。该框架在包含四个移动机器人的模拟迷宫环境中进行了评估。实验结果表明，所提出的价值引导扩散规划将智能体间干扰率从55.4%降低到41.8%，证明了在保持分散式轨迹生成可扩展性的同时可以有效实现协调。这些结果表明，基于多智能体强化学习的价值引导可以有效地将协调引入分散式生成规划器，而无需完全联合的多机器人模型。

## Abstract
Coordinating multiple robots in shared environments requires generating feasible trajectories for each agent while accounting for interactions among agents. Centralized planning approaches become difficult to scale as the number of robots increases, while decentralized approaches that allow each agent to plan independently do not inherently account for inter-agent interactions. This paper presents a framework for coordinated multi-robot motion planning that combines decentralized generative trajectory planning with multi-agent reinforcement learning (MARL)-based coordination. Each robot independently generates candidate trajectories using a diffusion model trained on single-agent motion data, leveraging the generative model's ability to produce feasible and diverse trajectories. To reduce conflicts between agents, a centralized value function trained via MARL guides the reverse diffusion process through gradient-based steering, enabling interaction-aware trajectory generation without centralized joint planning or retraining of the generative model. This guidance follows an exponential tilting formulation, in which the value function biases the denoising distribution toward trajectories with higher expected multi-agent return. The framework is evaluated in a simulated maze environment with four mobile robots. Experimental results show that the proposed value-guided diffusion planning reduces the inter-agent interference rate from 55.4% to 41.8%, demonstrating that coordination can be effectively achieved while preserving the scalability of decentralized trajectory generation. These results suggest that MARL-based value guidance can effectively introduce coordination into decentralized generative planners without requiring a fully joint multi-robot model.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **问题背景**：多个机器人在共享环境中执行任务时，各自独立规划的分散式方法虽然可扩展性强，但本质忽略智能体间的交互，容易导致轨迹冲突或相互干扰；集中式方法虽能显式建模交互，但随机器人数量增加，联合规划空间急剧膨胀，难以扩展。
- **研究动机**：需要一种既能保持分散式轨迹生成的独立性与可扩展性，又能提供有效协调机制的规划框架，避免集中联合规划或重新训练生成模型。
- **整体含义**：本文提出将单智能体扩散模型与多智能体强化学习（MARL）价值引导相结合，使各机器人自主生成多样可行轨迹的同时，通过集中式价值函数的梯度信号在推理阶段引入交互感知，从而在分散式框架下实现协调。

### 2. 方法论
#### 核心思想
- **分散式生成 + 集中式价值引导**：先利用单智能体运动数据训练扩散模型，获得轨迹先验分布；然后用 MARL 训练一个评估多智能体联合轨迹质量的集中式价值函数；在生成推理时，用价值函数的梯度对扩散的反向去噪过程进行引导，使轨迹偏向协调行为，而无需修改或重新训练扩散模型。

#### 关键技术细节
- **单智能体轨迹分布构建**：使用 PPO 在迷宫环境中训练导航策略，收集大量起始-目标之间的成功轨迹，作为扩散模型的训练数据集 \(D = \{\tau^{(k)}\}\)。
- **扩散模型训练**：采用条件扩散模型，学习轨迹分布 \(p_\theta(\tau \mid c)\)，其中条件 \(c = (x_{\text{start}}, x_{\text{goal}})\)。模型使用 Temporal U-Net 架构，通过噪声预测目标训练（MSE），规划时域 \(H=176\)，去噪步数 \(T=256\)。
- **分散式多智能体生成**：每个机器人独立用同一扩散模型抽取轨迹 \(\tau^i \sim p_\theta(\tau^i \mid c^i)\)，联合分布因子化为 \(\prod_i p_\theta(\tau^i \mid c^i)\)。
- **集中式价值函数**：在开放空间环境用 MAPPO 训练集中式评论家 \(V_\phi(X)\)，输入所有机器人的联合观测（拼接状态），评估联合轨迹的长期协调奖励。
- **价值引导采样**（指数倾斜）：定义引导分布 \(\tilde{p}_\theta(X) \propto p_\theta(X) \exp(\lambda V_\phi(X))\)。在去噪过程中，将均值的更新变为 \(\mu^{i}_t + \lambda \Sigma_t \nabla_i\)，其中 \(\nabla_i\) 是价值函数对联合轨迹中第 i 个智能体部分的梯度。实际上相当于在每个去噪步上加入协调漂移项。
- **整体流程**：算法1 描述了完整的价值引导多智能体运动生成过程：从噪声初始化并施加边界条件，迭代去噪，每次去噪前计算联合梯度并更新每个智能体的均值。

### 3. 实验设计
- **仿真平台**：NVIDIA Isaac Sim 5.1.0，使用 Isaac Lab 和 PyTorch，强化学习采用 skrl 库。
- **环境**：
  - **迷宫环境**（30 m × 30 m）：用于单智能体数据收集和最终多机器人测试，内部有墙体通道。
  - **开放空间环境**：用于 MARL 价值函数训练，不包含迷宫墙壁，仅训练四机器人保持相对偏移并朝共同目标前进的协调行为。
- **机器人模型**：Leatherback 四轮转向移动机器人，边界半径 0.254 m。
- **数据集**：单智能体在迷宫内通过训练好的 PPO 策略收集 2,800 条导航轨迹，用作扩散模型训练数据。
- **对比方法**：
  - 基线：纯扩散模型生成，不加任何引导。
  - 所提方法：MARL 价值引导的扩散生成。
- **评价指标**：
  - 主指标：智能体间干扰率 \(R_{\text{agent}}\)，定义为智能体间距低于阈值 \(\rho_{\text{agent}} = 0.508\,\text{m}\) 的时间步比例。
  - 辅助指标：障碍干扰率 \(R_{\text{obs}}\)，阈值 \(\rho_{\text{obs}} = 0.254\,\text{m}\)。
- **测试场景**：四机器人从迷宫左下区域出发到右上区域，各机器人起始和目标位置相对中心点偏移约 1.5 m，形成略有不同的路径。两种方法各重复 10 次，取平均。

### 4. 资源与算力
- 文中提到使用一块 **NVIDIA RTX-4090 GPU** 进行训练和推理。
- 未明确说明训练时长、GPU 小时数或其他具体计算资源用量。

### 5. 实验数量与充分性
- **实验组数**：仅对比了基线扩散与价值引导扩散两种配置，每个配置重复 10 次，报告平均干扰率。
- **消融实验**：未对关键超参数（如引导强度 \(\lambda\)）、不同 MARL 算法、不同价值函数架构、不同扩散规划时域等进行消融研究。
- **场景多样性**：仅在一种迷宫地图、固定机器人数量（4 个）下测试，未考察不同地图复杂度、不同障碍物密度、不同智能体数量的影响。
- **客观性与公平性**：对比方法在同一任务（相同起始-目标）、相同扩散模型下进行，较为公平；但 MARL 价值函数在缺乏墙体的开放空间训练，引入了一定的环境不一致性，可能影响协调表现。

### 6. 主要结论与发现
- 价值引导扩散规划将多机器人平均智能体间干扰率从 **55.4%** 降低到 **41.8%**，下降了 **13.6 个百分点**。
- 证明在推理阶段引入 MARL 价值函数梯度可以有效降低分散式生成轨迹之间的冲突，无需修改或重训练扩散模型。
- 从轨迹可视化看，引导后的机器人空间分离度更高，但仍遵循大致相同的全局路径。
- 同时观察到障碍干扰率从 4.6% 升至 7.6%，表明价值函数在缺乏障碍物约束的训练环境下，可能会造成轨迹靠近迷宫边界的副作用。

### 7. 优点
- **方法创新**：首次将扩散模型与集中式 MARL 价值函数相结合，实现“生成 + 引导”的协调式多机器人运动规划，兼顾分散式可扩展性和集中式协调能力。
- **不需要重新训练**：扩散模型完全基于单智能体数据训练，价值函数单独训练，推理阶段直接组合，具有即插即用的特点。
- **实验设置合理**：在逼真的物理仿真环境（Isaac Sim）中评估，采用真实的机器人模型，且提供了定量指标和可视化分析。
- **问题定位清晰**：准确指出分散式生成缺乏交互的痛点，并给出可量化的改善结果。

### 8. 不足与局限
- **场景单一**：仅在一种迷宫环境和固定机器人数量（4 个）下评估，未测试不同环境复杂度、更大规模智能体群。
- **训练不匹配**：MARL 价值函数在无障碍的开放空间训练，与最终应用的迷宫环境存在分布偏移，导致障碍干扰率恶化。
- **超参数未分析**：未探讨引导强度 \(\lambda\) 的影响及敏感性，也无法判断是否存在最优的平衡点。
- **轨迹质量**：引导后的轨迹出现局部振荡和靠近墙体现象，可能在实际部署中引入安全风险。
- **真实世界缺失**：仅在仿真中验证，未在物理机器人上实验。
- **扩展性未证明**：虽然声称可扩展，但未进行多机器人数量（比如 10、20 个）的对比实验。
- **算力细节不全**：未报告训练时长、内存消耗等信息，难以评估实际部署成本。

（完）
